---
const currentPath = Astro.url.pathname;
const base = import.meta.env.BASE_URL;

const isZh = currentPath.startsWith('/zh');
const langPrefix = isZh ? '/zh' : '';

// Helper to construct paths with base
const mkPath = (path) => {
  const cleanBase = base.endsWith('/') ? base.slice(0, -1) : base;
  return `${cleanBase}${path.startsWith('/') ? path : '/' + path}`;
};

const navLinks = [
  { text: isZh ? '首页' : 'Home', href: mkPath(langPrefix + '/') },
  { text: isZh ? '解决方案' : 'Solutions', href: mkPath(langPrefix + '/solutions/') },
  { text: isZh ? '愿景' : 'Vision', href: mkPath(langPrefix + '/vision/') },
  { text: isZh ? '开发者' : 'Developers', href: mkPath(langPrefix + '/developers/') },
];

const productsText = isZh ? '产品' : 'Products';
const computeText = isZh ? '⚡ 计算 & 推理' : '⚡ Compute & Inference';
const computeDesc = isZh ? 'OminiX Edge' : 'OminiX Edge';
const modelsText = isZh ? '🧠 模型' : '🧠 Models';
const modelsDesc = isZh ? 'Moxin LM & Voice' : 'Moxin LM & Voice';
const agentsText = isZh ? '🤖 智能体' : '🤖 Agents';
const agentsDesc = isZh ? 'MoFA 框架' : 'MoFA Framework';
const appText = isZh ? '💻 应用' : '💻 Application';
const appDesc = isZh ? 'Moly 客户端' : 'Moly Client';

const getLink = (path) => mkPath(langPrefix + path);

const switchLangPath = isZh 
  ? currentPath.replace('/zh', '') || '/' 
  : '/zh' + (currentPath === '/' ? '' : currentPath);
---

<nav class="glass-nav fixed top-0 left-0 right-0 z-50 px-6 py-4 transition-all duration-300">
  <div class="max-w-7xl mx-auto flex items-center justify-between">
    <a href={mkPath(langPrefix + "/")} class="flex items-center space-x-2 group">
      <img src={mkPath("/images/moxin.png")} alt="Moxin Intelligence" class="h-8 w-auto" />
      <span class="text-text-primary font-semibold text-xl tracking-tight">Moxin Organization</span>
    </a>

    <div class="hidden md:flex items-center space-x-8">
      <div class="relative group">
        <button class="text-text-secondary hover:text-text-primary transition-colors flex items-center space-x-1 text-base font-medium">
          <span>{productsText}</span>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
        </button>
        <!-- Dropdown -->
        <div class="absolute top-full left-0 mt-2 w-64 bg-white border border-gray-100 rounded-xl shadow-xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 transform origin-top-left p-2">
          <a href={getLink("/products/applications")} class="block p-3 rounded-lg hover:bg-gray-50 transition-colors">
            <div class="text-text-primary font-semibold text-sm">{appText}</div>
            <div class="text-xs text-text-secondary">{appDesc}</div>
          </a>
          <a href={getLink("/products/agents")} class="block p-3 rounded-lg hover:bg-gray-50 transition-colors">
            <div class="text-text-primary font-semibold text-sm">{agentsText}</div>
            <div class="text-xs text-text-secondary">{agentsDesc}</div>
          </a>
          <a href={getLink("/products/models")} class="block p-3 rounded-lg hover:bg-gray-50 transition-colors">
            <div class="text-text-primary font-semibold text-sm">{modelsText}</div>
            <div class="text-xs text-text-secondary">{modelsDesc}</div>
          </a>
          <a href={getLink("/products/compute")} class="block p-3 rounded-lg hover:bg-gray-50 transition-colors">
            <div class="text-text-primary font-semibold text-sm">{computeText}</div>
            <div class="text-xs text-text-secondary">{computeDesc}</div>
          </a>
        </div>
      </div>
      
      {navLinks.slice(1).map(link => (
        <a href={link.href} class="text-text-secondary hover:text-text-primary transition-colors text-base font-medium">{link.text}</a>
      ))}
    </div>

    <div class="flex items-center space-x-4">
      <a href={mkPath(switchLangPath)} class="text-text-secondary hover:text-text-primary transition-colors font-medium text-sm border border-gray-200 rounded-lg px-3 py-1 hover:bg-gray-50">
        {isZh ? 'EN' : '中文'}
      </a>
      <a href="https://github.com/moxin-org" target="_blank" class="text-text-secondary hover:text-text-primary transition-colors">
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd"></path></svg>
      </a>
      <a href="https://huggingface.co/moxin-org" target="_blank" class="text-text-secondary hover:text-text-primary transition-colors">
        <img src={mkPath("/images/hf-logo-pirate.svg")} alt="Hugging Face" class="w-5 h-5" />
      </a>
    </div>
  </div>
</nav>

<style is:global>
  .glass-nav {
    backdrop-filter: blur(20px);
    background: rgba(255, 255, 255, 0.8);
    border-bottom: 1px solid rgba(0, 0, 0, 0.05);
  }
</style>
